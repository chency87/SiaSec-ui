import Vue from 'vue';
import Vuex from 'vuex';
import defaultSettings from '@/settings';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    defaultSettings,
  },
});
